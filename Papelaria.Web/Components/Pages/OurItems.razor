@page "/items"
@* @inject IApiService ApiService *@

<h3>Items</h3>

@* este Item aqui precisa de referencia no bloco abaixo.
<MudTable Items="Items" Hover="true" Breakpoint="Breakpoint.Sm" Loading="@_loading" LoadingProgressColor="Color.Info">
    <HeaderContent>
        <MudTh>Name</MudTh>
        <MudTh>Description</MudTh>
        <MudTh>Qty Stock</MudTh>
        <MudTh>Selling To Consumer Price</MudTh>
        <MudTh>Buying From Supplier Price</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="Name">@context.Name</MudTd>
        <MudTd DataLabel="Description">@context.Description</MudTd>
        <MudTd DataLabel="QtyStock">@context.QtyStock</MudTd>
        <MudTd DataLabel="SellingToConsumerPrice">@context.SellingToConsumerPrice</MudTd>
        <MudTd DataLabel="BuyingFromSupplierPrice">@context.BuyingFromSupplierPrice</MudTd>
    </RowTemplate>
</MudTable>


@code {

    private string Message { get; set; } = string.Empty;

    private List<Shared.Item> Items { get; set; } = new();

    private bool _loading;

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();

        try
    }


} *@

